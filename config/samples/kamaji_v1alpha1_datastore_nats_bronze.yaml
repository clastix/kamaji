apiVersion: kamaji.clastix.io/v1alpha1
kind: DataStore
metadata:
  name: nats-bronze
spec:
  driver: nats
  endpoints:
    - nats://admin:test@bronze.nats-system.svc:4222
  basicAuth: null
  tlsConfig:
    certificateAuthority:
      certificate:
        secretReference:
          name: nats-bronze-config
          namespace: nats-system
          keyPath: ca.crt
    clientCertificate:
      certificate:
        secretReference:
          name: nats-bronze-config
          namespace: nats-system
          keyPath: server.crt
      privateKey:
        secretReference:
          name: nats-bronze-config
          namespace: nats-system
          keyPath: server.key
